import { View, Text } from "react-native";
import React from "react";
import { Entypo } from "@expo/vector-icons";

const CheckIcon = () => {
  return <Entypo name="check" size={18} color={"lightblue"} />;
};

export default CheckIcon;
